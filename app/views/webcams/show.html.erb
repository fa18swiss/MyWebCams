<p id="notice"><%= notice %></p>
<div class="col-lg-9 col-md-8 col-sm-7 col-xs-12">
  <a href="<%= @webcam.url %>">
    <img src="<%= @webcam.url %>?rand=<%=Random.rand()%>" class="img-responsive" alt="<%= @webcam.name %>" />
  </a>
</div>
<div class="col-lg-3 col-md-4 col-sm-5 col-xs-12">
  <p>
    <h1><%= @webcam.name %></h1>
  </p>


  <p>
    <strong>Latitude:</strong>
    <%= @webcam.latitude %>
  </p>

  <p>
    <strong>Longitude:</strong>
    <%= @webcam.longitude %>
  </p>

  <p>
    <strong>Orientation:</strong>
    <%if not @webcam.orientation.nil?%><%= @webcam.orientation %>°<%end%>
  </p>

  <p>
    <strong>User:</strong>
    <%= @webcam.user.username unless @webcam.user.nil? %>
  </p>
  <%= link_to 'Edit', edit_webcam_path(@webcam) %> |
  <%= link_to 'Back', webcams_path %>
</div>
<div class="col-xs-12">
  Comments
</div>


